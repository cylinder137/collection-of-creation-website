# 2026-09-03 工作日志

## 激活码发售思路转变 → 写入开发文档并推送 GitHub

**背景**：用户在本地累积了 30 个未推送提交（含 09-02 前端彻底重构 f211c4d 及后端骨架/业务改动，129 文件 +1.3 万行），远程 origin/main 落后。本次要求把"开发思路转变"落到开发文档并提交推送。

**产出**：
- 新增 `docs/激活码发售思路转变说明.md`（+109 行）：系统记录 09-02 业务模式核心转变——从「网页端在线发售激活码」转为「官网只做安装包下载 + 桌面端闭环授权（admin 权限采集机器码 + RSA 本地验签）」。含背景痛点表、目标架构时序图、前后端落地清单、新旧方案对照、当前状态与 coBrain 侧待办。
- 提交 `b0d452d`（docs: 新增激活码发售思路转变说明）。

**推送**：用户选择「直接推送本地 main」，共 31 个提交 push 到 `origin/main`（GitHub cylinder137）。`git ls-remote` 权威确认远程 main = b0d452d，与本地完全一致，工作区干净。推送前已用 .gitignore 与文件扫描确认未推送内容无 RSA 私钥/密码（.pem/.p12/cert/keys 均忽略）。

**坑/注意**：
- 本机 `refs/remotes/origin/main` 本地缓存 ref 显示陈旧值（240ef6e），与远程真实状态（b0d452d）不一致，属 packed-refs 本地缓存问题，无实际影响；判定以 `git ls-remote` 为准，勿被 `git log origin/main` 误导。
- git 中文文件名在 ls-tree/log 输出中是八进制转义形式，grep 中文会失配，用 `git ls-tree <commit> <dir>` 按目录查看即可。
- README「业务模式(2026-09 起)」节已在 09-02 提交中更新过，无需重复改。

## 隔夜休眠导致联调服务中断 → 已恢复（09:54）

- 现象：后端 java 进程退出，Hikari 报 `Thread starvation or clock leap detected (delta=8h27m)`；前端 5173 同时停摆。根因是笔记本隔夜休眠，非代码问题。
- 日志里大量 `NoResourceFoundException: No static resource .env / license.txt` —— 是外网扫描器在探测 Cloudflare 暴露的端口，被 GlobalExceptionHandler 正常接住，无害。
- 坑：重启时若旧进程仍占 8080 会报 "Port 8080 was already in use"，启动失败要先用 netstat -ano 确认占用 PID。
- 已恢复：后端 8080（health UP）、前端 5173（官网 200 / /admin 200），经代理的 admin 登录返回 token，证明 DB 链路在 clock leap 后已自愈。
