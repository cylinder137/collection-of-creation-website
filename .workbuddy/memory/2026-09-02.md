# 2026-09-02

## 造物集官网重构（激活码发售逻辑切换 + 前端重构 + 接入文档）

业务模式大转向：官网不再发售激活码，只提供 exe 自解压安装包下载；激活流程全部移到桌面客户端（安装程序提权采集机器码 → 后端签发激活码）。

**前端**（front_end）：
- 重写 HomeView + OfficialLayout（全新文案：产品下载 / 激活流程引导 / FAQ）
- 删除 ProductsView / PurchaseView / ActivationView / 旧 AdminView / DefaultLayout / ProductCard / utils/device.ts（浏览器机器码采集废弃）
- 新增隐藏管理后台：/admin 直达 AdminLoginView → AdminDashboardView（订单人工核验 / 激活码吊销 / 产品 CRUD）
- 鉴权：HMAC 无状态令牌缓存 localStorage，axios 拦截器对 /admin/** 自动附 Bearer，401 清令牌跳登录
- `npm run build`（vue-tsc）通过

**后端**（back_end）：
- AdminAuthInterceptor：验签后回库核验管理员存在且启用（每次请求）
- AdminController 新增产品 CRUD（GET/POST/PUT/PATCH status，价格元↔分）+ 激活码吊销 POST /admin/licenses/{id}/revoke
- ProductService 管理端方法带 @CacheEvict；ProductMapper/LicenseMapper 补 update 语句
- `mvn compile` 通过（注意：bash 里 mvn 报 classworlds 错误，须用 PowerShell 跑）

**文档**：docs/RSA与激活码接入文档.md（面向 coBrain：RSA 密钥体系 / 机器码规范 / 全流程接口 / Python·C# 验签示例 / 错误码表 / Checklist）；后端接口文档.md 补管理端与 license-key 接口节。

**待用户确认**：git 提交（团队规范要求 feature 分支 + PR，未代提交）。

## 本机联调环境（15:30）

- 注入初始管理员：admin / admin123（BCrypt 哈希，超管 role=1，id=1）
- MySQL root 密码见旧启动命令（target/backend.log 的来源进程），未入任何仓库文件
- 后端启动坑：沙箱环境变量 `SERVER__PORT=51270` 会覆盖 application.yml 的 server.port，启动时必须命令行显式 `--server.port=8080`；bash 里 mvn 报 classworlds 错误，要用 PowerShell 跑
- 后端运行方式：back_end/target 目录下，设好数据库密码环境变量后 `java -jar back-end-0.0.1-SNAPSHOT.jar --server.port=8080`（日志 backend.log）
- 前端 dev server 5173（Vite 代理 /api → 8080），HMR 自动加载新代码
- 验证通过：/api/health、admin 登录、Bearer 鉴权 /admin/me、/admin/products、无 token 返回 401、/api/license-key/public-key、经 5173 代理的完整登录链路
- 全链路冒烟测试七步全过：下单 → 待审核激活被正确拒绝 → 管理员核验 → 机器码激活（RSA 签发）→ 订单转已签发(4) → 本机核验 valid:true → 异机核验 valid:false。测试订单 202609021530566007758 及其激活码保留在库中供用户在管理后台查看效果
