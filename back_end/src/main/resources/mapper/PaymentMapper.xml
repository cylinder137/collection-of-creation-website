<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zaowuji.back.mapper.PaymentMapper">

    <sql id="allColumns">
        id, order_id, order_no, transaction_id, amount, status, notify_raw, paid_at, created_at
    </sql>

    <select id="selectByOrderNo" resultType="com.zaowuji.back.entity.Payment">
        SELECT <include refid="allColumns"/>
        FROM payment
        WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="com.zaowuji.back.entity.Payment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment (order_id, order_no, transaction_id, amount, status, notify_raw, paid_at)
        VALUES (#{orderId}, #{orderNo}, #{transactionId}, #{amount}, #{status}, #{notifyRaw}, #{paidAt})
    </insert>

</mapper>
